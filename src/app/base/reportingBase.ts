export class ReportingBase{
    
    export(payload:any){
        // so we now have an implementation which only requires a base class
        // to implment and then we have a feature which is reusable.
    }
}